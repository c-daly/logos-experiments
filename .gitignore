# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.uv/

# Test / lint caches
.pytest_cache/
.ruff_cache/

# Experiment run outputs — keep snapshots small; large artifacts stay local
*/workspace/*.tmp
*/workspace/*.log
# structural-edge-prediction run outputs: large/un-timestamped scratch, stay local
structural-edge-prediction/workspace/

# Tooling noise
.serena/
.idea/
.vscode/
.DS_Store

# embedding-representation: large local artifacts (cached vectors ~340MB; sample
# pull is regenerable via sample.py). Keep snapshots small.
embedding-representation/.cache/
embedding-representation/sample.json
