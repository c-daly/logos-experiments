[project]
name = "naming-driven-typing-experiment"
version = "0.1.0"
description = "Offline naming-driven typing experiment harness"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
